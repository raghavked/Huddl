/* Twitter/X card: same art as the Open Graph image, one source of truth. */
import OpengraphImage, { alt, contentType, size } from "./opengraph-image";

export { alt, contentType, size };
export default OpengraphImage;
